import AppTabs from '@/components/app-tabs';

/** The four roots. The Stack in ../_layout.tsx sits above this so sheets can cover it. */
export default function TabsLayout() {
  return <AppTabs />;
}
