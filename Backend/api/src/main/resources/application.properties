#Database Connection
spring.datasource.url=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.cyvdoferjcnkjbtbjrms
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

#JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

#Server
server.port=${PORT:8080}

#JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

#AI Quote Agent (Phase 9)
# Off by default — set QUOTE_AGENT_ENABLED=true on Railway once ANTHROPIC_API_KEY is set.
quote.agent.enabled=${QUOTE_AGENT_ENABLED:false}
# Month the cycle starts on Motherhood (#1); cycles every 6 months from here.
quote.agent.anchor-month=2026-08
quote.agent.batch-size=50
quote.agent.max-in-house=20
anthropic.api.key=${ANTHROPIC_API_KEY:}
anthropic.api.model=claude-opus-4-8
anthropic.api.max-tokens=8000